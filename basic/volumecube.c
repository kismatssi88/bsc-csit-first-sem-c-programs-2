#include<stdio.h>
#include<conio.h>
int main()
    {
    int l,V;
    printf("enter the length of a cube");
    scanf("%d",&l);
    V = l*l*l;
    printf("the volume of cube is %d",V);
    return 0;
    }
