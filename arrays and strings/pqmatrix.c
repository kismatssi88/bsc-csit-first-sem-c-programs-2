#include<stdio.h>
#include<conio.h>
int main()
{
    int i,j,p,q;
    printf("enter no. of rows(p)");
    scanf("%d",&p);
    printf("enter no. of columns(q)");
    scanf("%d",&q);
    int matrix[p][q];
    

}